Completados:
		-Base de Datos:
		*Tabla de usuarios <- Completo
		*Tabla de tipos de usuario <-Completo
		** Considerar una tabla para bitacora <- Completo

	-Programa base:
		*Conexion con base de datos <- Completado
		*Pedir lectura de datos <- Completado



To DOOMS:

	-Base de Datos:

		** Considerar un PROCEDURE para insertar y obtener datos a la vez <- Necesario

	-Programa base:

		*Verificar datos en la BD <- Parcial
		*Agregar a la bitacora ( Es necesario el Procedure )
		*Impresion de salida <- Parcial
		*Interfraz <- Parcial
			** Falta hacer una ventana para el caso en el que entre algun visitante

	-Requerimientos para los archivos .py:
		*Driver de python para conectarse con mysql
			python -m pip install mysql-connector
